# ProseForge Workbench — Environment Configuration
#
# Copy to .env.dev, .env.demo, or .env.prod and fill in values.
# Environment files are gitignored — never commit API keys.

# ProseForge API base URL
PROSEFORGE_URL=

# API token (Bearer token for authentication)
PROSEFORGE_TOKEN=
